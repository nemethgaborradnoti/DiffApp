<Window x:Class="DiffApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:DiffApp.Converters"
        xmlns:models="clr-namespace:DiffApp.Models"
        xmlns:views="clr-namespace:DiffApp.Views"
        xmlns:fonts="clr-namespace:Fonts"
        xmlns:viewModels="clr-namespace:DiffApp.ViewModels"
        mc:Ignorable="d"
        Title="{DynamicResource MainWindow_Title}" Height="800" Width="1200"
        MinHeight="500" MinWidth="700"
        WindowStartupLocation="Manual"
        SourceInitialized="Window_SourceInitialized"
        Closing="Window_Closing">

    <Window.Resources>
        <Style x:Key="NavButtonStyle" TargetType="RadioButton" BasedOn="{StaticResource {x:Type ToggleButton}}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="{StaticResource GrayTextBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}" BorderThickness="0">
                            <Grid>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                <Rectangle x:Name="Indicator" Height="3" VerticalAlignment="Bottom" Fill="Transparent"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource BrushPrimary}"/>
                                <Setter TargetName="Indicator" Property="Fill" Value="{StaticResource BrushAccent}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource BrushPrimary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource BrushSurface}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" BorderBrush="{StaticResource BrushBorder}" BorderThickness="0,0,0,1" Padding="15,0">
            <Grid Height="48">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,30,0">
                    <TextBlock Text="{x:Static fonts:MaterialIcons.difference}" Style="{StaticResource IconStyle}" FontSize="24" Foreground="{StaticResource BrushAccent}" Margin="0,0,10,0"/>
                    <TextBlock Text="{DynamicResource MainWindow_Title}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource BrushPrimary}" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <RadioButton Content="{DynamicResource MainWindow_NavEditor}"
                                 GroupName="Nav"
                                 Command="{Binding NavigateToEditorCommand}"
                                 Style="{StaticResource NavButtonStyle}"
                                 IsChecked="{Binding CurrentViewModel, Converter={StaticResource EnumMatchToBooleanConverter}, ConverterParameter={x:Type viewModels:EditorViewModel}, Mode=OneWay}"/>

                    <RadioButton Content="{DynamicResource MainWindow_NavHistory}"
                                 GroupName="Nav"
                                 Command="{Binding NavigateToHistoryCommand}"
                                 Style="{StaticResource NavButtonStyle}"
                                 IsChecked="{Binding CurrentViewModel, Converter={StaticResource EnumMatchToBooleanConverter}, ConverterParameter={x:Type viewModels:HistoryViewModel}, Mode=OneWay}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="{StaticResource BrushBackground}">
            <ContentControl Content="{Binding CurrentViewModel}"/>
        </Border>
    </Grid>
</Window>
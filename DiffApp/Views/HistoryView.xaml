<UserControl x:Class="DiffApp.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:DiffApp.Views"
             xmlns:viewModels="clr-namespace:DiffApp.ViewModels"
             xmlns:fonts="clr-namespace:Fonts"
             xmlns:converters="clr-namespace:DiffApp.Converters"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             Background="{StaticResource BrushBackground}"
             UseLayoutRounding="True"
             SnapsToDevicePixels="True">

    <UserControl.Resources>
        <SolidColorBrush x:Key="BrushLightBlueBorder" Color="#D0E3F0"/>

        <Style x:Key="DeleteIconButtonStyle" TargetType="Button" BasedOn="{StaticResource IconButtonStyle}">
            <Setter Property="Foreground" Value="{StaticResource DangerBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource HoverBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="BookmarkIconButtonStyle" TargetType="Button" BasedOn="{StaticResource IconButtonStyle}">
            <Setter Property="Foreground" Value="{StaticResource BrushAccent}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource HoverBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{StaticResource BrushSurface}"
                BorderBrush="{StaticResource BrushBorder}"
                BorderThickness="0,0,0,1"
                Padding="20,15">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="{x:Static fonts:MaterialIcons.history}" Style="{StaticResource IconStyle}" FontSize="24" Margin="0,0,10,0" Foreground="{StaticResource GrayTextBrush}"/>
                    <TextBlock Text="{DynamicResource HistoryView_Title}" FontSize="20" FontWeight="Bold" Foreground="{StaticResource BrushPrimary}"/>
                </StackPanel>

                <Button HorizontalAlignment="Right"
                        Style="{StaticResource DangerButton}"
                        Command="{Binding DeleteAllCommand}"
                        Visibility="{Binding HasItems, Converter={StaticResource VisibilityConverter}}"
                        ToolTip="{DynamicResource HistoryView_ClearAllHistoryToolTip}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{x:Static fonts:MaterialIcons.delete}" Style="{StaticResource IconStyle}" FontSize="18" Foreground="White" Margin="0,0,5,0"/>
                        <TextBlock Text="{DynamicResource HistoryView_DeleteAll}" Foreground="White" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Panel.ZIndex="1">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HistoryItems.Count}" Value="0">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>

            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="{x:Static fonts:MaterialIcons.history_toggle_off}"
                           Style="{StaticResource IconStyle}"
                           FontSize="64"
                           Foreground="{StaticResource BrushBorder}"
                           Margin="0,0,0,20"/>
                <TextBlock Text="{DynamicResource HistoryView_NoHistoryYet}"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource GrayTextBrush}"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="{DynamicResource HistoryView_ComparisonsWillAppear}"
                           FontSize="14"
                           Foreground="{StaticResource GrayTextBrush}"
                           HorizontalAlignment="Center"
                           Margin="0,5,0,0"/>
            </StackPanel>
        </Grid>

        <ListView Grid.Row="1"
                  ItemsSource="{Binding HistoryItems}"
                  HorizontalContentAlignment="Stretch"
                  ScrollViewer.CanContentScroll="True"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                  BorderThickness="0"
                  Padding="10">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Margin" Value="0,0,0,8"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <Border x:Name="CardBorder"
                                        Background="White"
                                        BorderBrush="{StaticResource BrushLightBlueBorder}"
                                        BorderThickness="1"
                                        Margin="1"
                                        CornerRadius="6"
                                        Padding="5">

                                    <Border.InputBindings>
                                        <MouseBinding MouseAction="LeftClick"
                                                      Command="{Binding DataContext.RestoreItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                      CommandParameter="{Binding}"/>
                                    </Border.InputBindings>

                                    <Grid>
                                        <Border VerticalAlignment="Stretch"
                                                HorizontalAlignment="Stretch"
                                                IsHitTestVisible="False"
                                                Opacity="0.25">
                                            <Border.Background>
                                                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                                    <GradientStop Color="Transparent" Offset="0"/>
                                                    <GradientStop Color="{StaticResource ColorAccent}" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Border.Background>
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsBookmarked}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                        </Border>

                                        <Grid Margin="10,5">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Grid.Column="0"
                                                       Text="{Binding RelativeTime}"
                                                       FontSize="12"
                                                       MinWidth="70"
                                                       Foreground="{StaticResource BrushAccent}"
                                                       FontWeight="Bold"
                                                       VerticalAlignment="Center"
                                                       TextTrimming="CharacterEllipsis"/>

                                            <Grid Grid.Column="1" Margin="15,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="30"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <!-- Original Side -->
                                                <Grid Grid.Column="0">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto" />
                                                        <ColumnDefinition Width="*" />
                                                    </Grid.ColumnDefinitions>

                                                    <TextBlock Grid.Column="0" Foreground="{StaticResource DangerBrush}" FontSize="11" FontWeight="SemiBold" Margin="0,0,8,0" VerticalAlignment="Center">
                                                        <Run Text="{Binding OriginalLineCount, Mode=OneWay}" />
                                                        <Run Text=" " />
                                                        <Run Text="{DynamicResource ComparisonView_Lines}" />
                                                    </TextBlock>

                                                    <Border Grid.Column="1"
                                                            Background="{StaticResource BrushSurface}"
                                                            CornerRadius="4"
                                                            Padding="8,6"
                                                            VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding OriginalPreview}"
                                                                   FontFamily="{StaticResource CodeFont}"
                                                                   FontSize="12"
                                                                   Foreground="{StaticResource GrayTextBrush}"
                                                                   TextWrapping="NoWrap"
                                                                   TextTrimming="CharacterEllipsis"/>
                                                    </Border>
                                                </Grid>

                                                <TextBlock Grid.Column="1"
                                                           Text="{x:Static fonts:MaterialIcons.arrow_forward}"
                                                           Style="{StaticResource IconStyle}"
                                                           FontSize="16"
                                                           Foreground="{StaticResource GrayTextBrush}"
                                                           VerticalAlignment="Center"
                                                           HorizontalAlignment="Center"/>

                                                <!-- Modified Side -->
                                                <Grid Grid.Column="2">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto" />
                                                        <ColumnDefinition Width="*" />
                                                    </Grid.ColumnDefinitions>

                                                    <TextBlock Grid.Column="0" Foreground="{StaticResource SuccessBrush}" FontSize="11" FontWeight="SemiBold" Margin="0,0,8,0" VerticalAlignment="Center">
                                                        <Run Text="{Binding ModifiedLineCount, Mode=OneWay}" />
                                                        <Run Text=" " />
                                                        <Run Text="{DynamicResource ComparisonView_Lines}" />
                                                    </TextBlock>

                                                    <Border Grid.Column="1"
                                                            Background="{StaticResource BrushSurface}"
                                                            CornerRadius="4"
                                                            Padding="8,6"
                                                            VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding ModifiedPreview}"
                                                                   FontFamily="{StaticResource CodeFont}"
                                                                   FontSize="12"
                                                                   Foreground="{StaticResource TextBrush}"
                                                                   FontWeight="SemiBold"
                                                                   TextWrapping="NoWrap"
                                                                   TextTrimming="CharacterEllipsis"/>
                                                    </Border>
                                                </Grid>
                                            </Grid>

                                            <StackPanel Grid.Column="2"
                                                        Orientation="Horizontal"
                                                        VerticalAlignment="Center">

                                                <Button Style="{StaticResource BookmarkIconButtonStyle}"
                                                        ToolTip="{DynamicResource HistoryView_BookmarkToolTip}"
                                                        Command="{Binding DataContext.ToggleBookmarkCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"
                                                        Margin="0,0,5,0">
                                                    <TextBlock FontSize="24"
                                                               Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock" BasedOn="{StaticResource IconStyle}">
                                                                <Setter Property="Text" Value="{x:Static fonts:MaterialIcons.bookmark_border}"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsBookmarked}" Value="True">
                                                                        <Setter Property="Text" Value="{x:Static fonts:MaterialIcons.bookmark}"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </Button>

                                                <Button Style="{StaticResource DeleteIconButtonStyle}"
                                                        ToolTip="{DynamicResource HistoryView_DeleteToolTip}"
                                                        Command="{Binding DataContext.DeleteItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}">
                                                    <TextBlock Text="{x:Static fonts:MaterialIcons.delete}"
                                                               Style="{StaticResource IconStyle}"
                                                               FontSize="24"
                                                               Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                                </Button>
                                            </StackPanel>
                                        </Grid>
                                    </Grid>

                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="5" ShadowDepth="1" Direction="270" Color="#000000" Opacity="0.05"/>
                                    </Border.Effect>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="CardBorder" Property="BorderBrush" Value="{StaticResource BrushAccent}"/>
                                        <Setter TargetName="CardBorder" Property="Background" Value="White"/>
                                        <Setter TargetName="CardBorder" Property="BorderThickness" Value="2"/>
                                        <Setter TargetName="CardBorder" Property="Margin" Value="0"/>
                                    </Trigger>

                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="CardBorder" Property="BorderBrush" Value="{StaticResource BrushAccent}"/>
                                        <Setter TargetName="CardBorder" Property="Background" Value="White"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
    </Grid>
</UserControl>
<UserControl x:Class="DiffApp.Views.InputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:DiffApp.Converters"
             xmlns:fonts="clr-namespace:Fonts"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="1000"
             Background="{StaticResource BrushBackground}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Grid Grid.Row="0" Margin="0,0,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="{x:Static fonts:MaterialIcons.description}" 
                           Style="{StaticResource IconStyle}" 
                           Foreground="{StaticResource GrayTextBrush}" 
                           FontSize="16"
                           Margin="0,0,8,0"/>
                <TextBlock Text="Original Text" 
                           Style="{StaticResource HeaderTextStyle}" 
                           FontSize="14"/>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="{x:Static fonts:MaterialIcons.edit_note}" 
                           Style="{StaticResource IconStyle}" 
                           Foreground="{StaticResource GrayTextBrush}" 
                           FontSize="16"
                           Margin="0,0,8,0"/>
                <TextBlock Text="Changed Text" 
                           Style="{StaticResource HeaderTextStyle}" 
                           FontSize="14"/>
            </StackPanel>
        </Grid>

        <!-- Input TextBoxes -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Original Text Input -->
            <TextBox Grid.Column="0"
                     Text="{Binding LeftText, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource ModernTextBoxStyle}"
                     AcceptsReturn="True"
                     AcceptsTab="True"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     TextWrapping="{Binding IsWordWrapEnabled, Converter={x:Static converters:BoolToTextWrappingConverter.Instance}}"
                     FontFamily="Consolas"
                     FontSize="13"
                     VerticalContentAlignment="Top"
                     Padding="8"
                     MaxHeight="400"
                     VerticalAlignment="Stretch"/>

            <!-- Changed Text Input -->
            <TextBox Grid.Column="2"
                     Text="{Binding RightText, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource ModernTextBoxStyle}"
                     AcceptsReturn="True"
                     AcceptsTab="True"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     TextWrapping="{Binding IsWordWrapEnabled, Converter={x:Static converters:BoolToTextWrappingConverter.Instance}}"
                     FontFamily="Consolas"
                     FontSize="13"
                     VerticalContentAlignment="Top"
                     Padding="8"
                     MaxHeight="400"
                     VerticalAlignment="Stretch"/>
        </Grid>

        <!-- Footer Action -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Command="{Binding FindDifferenceCommand}"
                    Style="{StaticResource PrimaryButton}"
                    Padding="25,10"
                    FontSize="14">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{x:Static fonts:MaterialIcons.compare_arrows}" 
                               Style="{StaticResource IconStyle}" 
                               Foreground="White" 
                               FontSize="18"
                               Margin="0,0,8,0"/>
                    <TextBlock Text="Find Difference" FontWeight="SemiBold"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
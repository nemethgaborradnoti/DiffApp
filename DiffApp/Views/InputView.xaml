<UserControl x:Class="DiffApp.Views.InputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:DiffApp.Converters"
             xmlns:fonts="clr-namespace:Fonts"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Input -->
            <Grid Grid.Column="0" Margin="0,5,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Original Text" FontWeight="SemiBold" Foreground="#586069" Margin="5,0,0,5"/>
                <TextBox Grid.Row="1"
                         Text="{Binding LeftText, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         TextWrapping="{Binding IsWordWrapEnabled, Converter={x:Static converters:BoolToTextWrappingConverter.Instance}}"
                         FontFamily="Consolas"
                         Padding="5"/>
            </Grid>

            <!-- Middle Spacer (Button removed) -->
            <Grid Grid.Column="1">
            </Grid>

            <!-- Right Input -->
            <Grid Grid.Column="2" Margin="0,5,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Changed Text" FontWeight="SemiBold" Foreground="#586069" Margin="5,0,0,5"/>
                <TextBox Grid.Row="1"
                         Text="{Binding RightText, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         TextWrapping="{Binding IsWordWrapEnabled, Converter={x:Static converters:BoolToTextWrappingConverter.Instance}}"
                         FontFamily="Consolas"
                         Padding="5"/>

                <!-- Manual Collapse Button -->
                <Button Grid.Row="0"
                        Style="{StaticResource IconButtonStyle}"
                        Command="{Binding DataContext.ToggleInputPanelCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Margin="0,-5,0,0"
                        Padding="5"
                        ToolTip="Collapse Input Panel">
                    <TextBlock Text="{x:Static fonts:MaterialIcons.keyboard_arrow_down}" Style="{StaticResource IconStyle}"/>
                </Button>
            </Grid>
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,10">
            <Button Command="{Binding FindDifferenceCommand}" Padding="20,10">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="FIND DIFFERENCE" FontWeight="Bold"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
<UserControl x:Class="DiffApp.Views.InputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:DiffApp.Converters"
             xmlns:fonts="clr-namespace:Fonts"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="0,5,2,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Original Text" FontWeight="SemiBold" Foreground="{StaticResource GrayTextBrush}" Margin="5,0,0,5"/>
                <TextBox Grid.Row="1"
                         Text="{Binding LeftText, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         TextWrapping="{Binding IsWordWrapEnabled, Converter={x:Static converters:BoolToTextWrappingConverter.Instance}}"
                         FontFamily="Consolas"
                         Padding="5"/>
            </Grid>

            <Grid Grid.Column="1" Margin="2,5,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <TextBlock Text="Changed Text" FontWeight="SemiBold" Foreground="{StaticResource GrayTextBrush}" Margin="5,0,0,5" VerticalAlignment="Center"/>

                    <Button Style="{StaticResource IconButtonStyle}"
                             Command="{Binding DataContext.ToggleInputPanelCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                             HorizontalAlignment="Right"
                             VerticalAlignment="Center"
                             Margin="0,-4,0,0"
                             Padding="4"
                             ToolTip="Collapse Input Panel">
                        <TextBlock Text="{x:Static fonts:MaterialIcons.keyboard_arrow_down}" Style="{StaticResource IconStyle}"/>
                    </Button>
                </Grid>

                <TextBox Grid.Row="1"
                         Text="{Binding RightText, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         TextWrapping="{Binding IsWordWrapEnabled, Converter={x:Static converters:BoolToTextWrappingConverter.Instance}}"
                         FontFamily="Consolas"
                         Padding="5"/>
            </Grid>
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,10">
            <Button Command="{Binding FindDifferenceCommand}" 
                    Padding="20,10"
                    Style="{StaticResource PrimaryButton}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="FIND DIFFERENCE" FontWeight="Bold"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>